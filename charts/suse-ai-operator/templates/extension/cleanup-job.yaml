{{- if .Values.extension.enable }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "suse-ai-operator.fullname" . }}-cleanup
  namespace: {{ include "suse-ai-operator.namespaceName" . }}
  labels:
    {{- include "suse-ai-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 360
  ttlSecondsAfterFinished: 60
  template:
    spec:
      serviceAccountName: {{ include "suse-ai-operator.fullname" . }}-cleanup
      restartPolicy: Never
      {{- include "suse-ai-operator.imagePullSecrets" . | nindent 6 }}
      securityContext:
        {{- toYaml .Values.extension.cleanup.podSecurityContext | nindent 8 }}
      containers:
        - name: cleanup
          image: "{{ .Values.extension.cleanup.image.registry }}/{{ .Values.extension.cleanup.image.repository }}:{{ .Values.extension.cleanup.image.tag }}"
          imagePullPolicy: {{ .Values.extension.cleanup.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.extension.cleanup.securityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.extension.cleanup.resources | nindent 12 }}
          command:
            - /bin/sh
            - -c
            - |
              set -eu
              CR_NAME="{{ .Values.extension.crName | default "suseai" }}"
              echo "Checking if InstallAIExtension CR exists..."
              if ! kubectl get installaiextension "$CR_NAME" > /dev/null 2>&1; then
                echo "CR not found, nothing to clean up"
                exit 0
              fi
              echo "Deleting InstallAIExtension CR..."
              if ! kubectl delete installaiextension "$CR_NAME" --timeout=300s; then
                echo "ERROR: Delete timed out. The operator finalizer did not complete cleanup."
                echo "Remaining CR state:"
                kubectl get installaiextension "$CR_NAME" -o yaml 2>/dev/null || true
                echo ""
                echo "The operator is still running and may complete cleanup. Retry 'helm uninstall' later."
                echo "To force removal after verifying no resources are leaked:"
                echo "  kubectl patch installaiextension $CR_NAME --type=merge -p '{\"metadata\":{\"finalizers\":[]}}'"
                exit 1
              fi
              echo "Cleanup complete"
{{- end }}
